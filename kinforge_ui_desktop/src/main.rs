/// Kinforge Desktop UI
///
/// The desktop GUI is planned for a future release. For now, use the CLI:
///   kinforge --help
fn main() {
    eprintln!("Kinforge Desktop UI is not yet implemented.");
    eprintln!("Please use the CLI: kinforge --help");
    std::process::exit(1);
}
